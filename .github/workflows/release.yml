# Release workflow - creates GitHub release on version tags
# NOTE: Docker build and push steps are commented out due to org package permissions.
# Uncomment the build-and-push job when you have permission to push to ghcr.io
name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.4.0)'
        required: true
        type: string

jobs:
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run unit tests
        run: make test-unit

  # NOTE: Docker build and push job is commented out due to org package permissions.
  # Uncomment this job when you have permission to push to ghcr.io
  #
  # build-and-push:
  #   name: Build and Push
  #   runs-on: ubuntu-latest
  #   needs: test
  #   permissions:
  #     contents: read
  #     packages: write
  #   env:
  #     REGISTRY: ghcr.io
  #     IMAGE_NAME: ${{ github.repository }}
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up QEMU
  #       uses: docker/setup-qemu-action@v3
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Extract version
  #       id: version
  #       run: |
  #         if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
  #           VERSION="${{ inputs.version }}"
  #         else
  #           VERSION="${GITHUB_REF#refs/tags/}"
  #         fi
  #         echo "version=${VERSION}" >> $GITHUB_OUTPUT
  #
  #     - name: Build and push butler image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: docker/Dockerfile
  #         target: butler
  #         platforms: linux/amd64,linux/arm64
  #         push: true
  #         tags: |
  #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
  #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  #         build-args: |
  #           VERSION=${{ steps.version.outputs.version }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
  #
  #     - name: Build and push butler-debug image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: docker/Dockerfile
  #         target: butler-debug
  #         platforms: linux/amd64,linux/arm64
  #         push: true
  #         tags: |
  #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-debug:${{ steps.version.outputs.version }}
  #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-debug:latest
  #         build-args: |
  #           VERSION=${{ steps.version.outputs.version }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.version.outputs.version }}
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
